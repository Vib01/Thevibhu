{
  "name": "personal-website",
  "version": "1.0.0",
  "description": "Personal website with testing setup",
  "scripts": {
    "start": "python -m http.server 8000",
    "update:strava": "node scripts/fetch-strava-stats.js",
    "test": "jest",
    "test:e2e": "playwright test",
    "test:visual": "playwright test --config=visual.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "jest": "^29.7.0",
    "jest-puppeteer": "^8.0.4"
  }
}
