export interface ModelInfo {
  cohort_min: number;
  cohort_max: number;
  age_min: number;
  age_max: number;
  n_training_records: number;
  n_cohorts: number;
  sex: string;
  species: string;
  stock_area: string;
  reference: string;
}
